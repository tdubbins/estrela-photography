# Local-only planning & briefing files — not part of the public website
notes.md
SPEC.md
content-needed.md
content-needed.pdf

# Esther's original-resolution photos & raw working files — never commit
# (web-optimized copies live in ./images/ and are committed)
content/
lighthouse reports/
wetransfer_*/
Adjustments*.pdf

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
