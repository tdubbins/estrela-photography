# Local-only working material — originals, briefs, reports.
# Not part of the public website; never commit.
# (web-optimized photo copies live in ./images/ and ARE committed)
_working/

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
